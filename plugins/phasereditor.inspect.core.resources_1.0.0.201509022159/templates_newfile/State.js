/**
 * {class-name} state.
 */
function {class-name}() {
	Phaser.State.call(this);
	// TODO: generated method.
}

/** @type Phaser.State */
var proto = Object.create(Phaser.State);
{class-name}.prototype = proto;

{class-name}.prototype.preload = function() {
	// TODO: generated method.
};

{class-name}.prototype.create = function() {
	// TODO: generated method.
};

{class-name}.prototype.update = function() {
	// TODO: generated method.
};